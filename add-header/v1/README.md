# add-header/v1

This is version 1 of the add-header module.

## API

- `AddHeader(w http.ResponseWriter, key, value string)`: Adds a single header to the response.

## Breaking Changes

This is the initial version, no breaking changes.